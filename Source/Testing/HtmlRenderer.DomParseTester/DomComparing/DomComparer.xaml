<Window
    x:Class="HtmlRenderer.DomParseTester.DomComparing.DomComparer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:HtmlRenderer.DomParseTester.DomComparing"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="DOM Comparer"
    Name="DomComparerElement"
    Width="1200"
    Height="700"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Menu
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="2">
            <MenuItem Header="Left">
                <MenuItem Command="{Binding LeftOpenDomCommand}" Header="Open DOM" />
                <MenuItem Command="{Binding LeftOpenHtmlCommand}" Header="Open HTML" />
                <Separator />
                <MenuItem Header="Recent" ItemsSource="{Binding RecentItems}" Click="OpenRecentLeft_Click" />
            </MenuItem>
            <MenuItem Header="Right">
                <MenuItem Command="{Binding RightOpenDomCommand}" Header="Open DOM" />
                <MenuItem Command="{Binding RightOpenHtmlCommand}" Header="Open HTML" />
                <Separator />
                <MenuItem Header="Recent" ItemsSource="{Binding RecentItems}" Click="OpenRecentRight_Click" />
            </MenuItem>
        </Menu>

        <local:DomTree
            x:Name="LeftTree"
            Grid.Row="1"
            Grid.Column="0"
            Margin="0,0,2,2"
            RootNode="{Binding Path=DataContext.Left, ElementName=DomComparerElement}" />
        <local:DomTree
            x:Name="RightTree"
            Grid.Row="1"
            Grid.Column="1"
            Margin="2,0,0,2"
            RootNode="{Binding Path=DataContext.Right, ElementName=DomComparerElement}" />

        <GridSplitter
            Grid.Row="1"
            Grid.RowSpan="3"
            Grid.Column="0"
            Width="4"
            Margin="0,0,-2,0"
            HorizontalAlignment="Right"
            VerticalAlignment="Stretch"
            Background="Transparent" />
        <GridSplitter
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Height="4"
            Margin="0,0,0,-2"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Background="Transparent" />
        <GridSplitter
            Grid.Row="2"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Height="4"
            Margin="0,0,0,-2"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Background="Transparent" />

        <local:PropertiesList
            Grid.Row="2"
            Grid.Column="0"
            Margin="0,2,2,2"
            DomNode="{Binding ElementName=LeftTree, Path=SelectedNode}" />
        <local:PropertiesList
            Grid.Row="2"
            Grid.Column="1"
            Margin="2,2,0,2"
            DomNode="{Binding ElementName=RightTree, Path=SelectedNode}" />

        <local:AttributesList
            Grid.Row="3"
            Grid.Column="0"
            Margin="0,2,2,0"
            DomNode="{Binding ElementName=LeftTree, Path=SelectedNode}" />
        <local:AttributesList
            Grid.Row="3"
            Grid.Column="1"
            Margin="2,2,0,0"
            DomNode="{Binding ElementName=RightTree, Path=SelectedNode}" />
    </Grid>
</Window>
